{
  "name": "dutygrid-workspace",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --prefix apps/web && cp -R apps/web/.vercel .vercel",
    "install": "npm install --prefix apps/web"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "dotenv": "^17.3.1",
    "pg": "^8.18.0"
  }
}